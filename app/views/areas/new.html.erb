<div class="static">
<div data-country-id="<%= @current_country.id %>" data-country-iso="<%= @current_country.iso %>" id="map"></div>

  <div id="sidebar" class="shadow">
    <div class="container-fluid">
      <div class="row-fluid">
        <div class="span12">
          <h5>New area</h5>

          <%= render 'form' %>
        </div>
      </div>
    </div>
  </div>
</div>
